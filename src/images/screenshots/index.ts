// Central manifest for real Kleo app screenshots.
// Each slot is currently seeded with the onboarding screen as a placeholder —
// replace the matching PNG in this folder with the real screen to update the site.
import cards from './cards.png'
import home from './home.png'
import investments from './investments.png'
import onboarding from './onboarding.png'
import spending from './spending.png'
import tips from './tips.png'
import transactions from './transactions.png'

export const screenshots = {
  onboarding,
  home,
  transactions,
  investments,
  cards,
  spending,
  tips,
}
